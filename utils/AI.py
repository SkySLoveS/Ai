import json
import requests
from core.config import Settings

config = Settings()

class AI:

    def __init__(self):
        self.base_url = "https://openrouter.ai/api/v1/chat/completions"
        self.headers = {
            "Authorization": f"Bearer {config.AI_token}",
            "Content-Type": "application/json"
        }

    def request(self, content: str):
        data = {
            "model": "inclusionai/ling-3.0-flash-vl:free",
            "messages": [
                {
                    "role": "user",
                    "content": (
                        """
                        Компания-интегратор внедряет бизнес-систему Saby.

                        Тебе передаётся транскрипт одного разговора между менеджером интегратора и клиентом.
                        Твоя задача — извлечь из транскрипта только фактическую информацию и вернуть её в строго заданной структуре.

                        Верни ТОЛЬКО один валидный JSON-объект со следующими ключами:

                        {
                            "result": "...",
                            "next_step": "...",
                            "date": "...",
                            "needs": "...",
                            "risks": "...",
                            "managers_mistakes": "...",
                            "important": "..."
                        }

                        Правила формата:
                        1. Названия ключей должны совпадать абсолютно точно.
                        2. Не добавляй и не удаляй ключи.
                        3. Все значения должны быть строками.
                        4. Все значения должны быть на русском языке.
                        5. Не используй Markdown.
                        6. Не используй ```json или другие блоки кода.
                        7. Не пиши пояснений до или после JSON.
                        8. JSON должен быть валидным и корректно парситься через json.loads().
                        9. Если для поля нет достаточной информации в транскрипте, используй ровно:
                        "Информация отсутствует".

                        ОСНОВНОЕ ПРАВИЛО:
                        Извлекай информацию из транскрипта, а не делай выводы о том, что, вероятно, произойдёт дальше.

                        Запрещено:
                        - придумывать факты;
                        - додумывать намерения участников;
                        - добавлять причинно-следственные связи, которых нет в разговоре;
                        - превращать предположение в факт;
                        - превращать намерение в принятое решение;
                        - превращать возможность в договорённость;
                        - превращать готовность обсудить что-либо в уже принятое решение;
                        - приписывать участникам действия или высказывания, которых не было;
                        - добавлять оценку ситуации от себя.
                        - Вычислять дату
                        
                        Разрешено:
                        - сокращать длинные высказывания;
                        - объединять несколько связанных фактов;
                        - переформулировать высказывания без изменения их смысла;
                        - удалять повторы;
                        - сохранять имена, даты, суммы, роли и другие конкретные данные из транскрипта.

                        ОПРЕДЕЛЕНИЯ ПОЛЕЙ:

                        "result":
                        Краткий фактический итог разговора.
                        Укажи, к чему участники реально пришли во время разговора.
                        Не описывай предполагаемые будущие события как уже принятые решения.
                        Если решение не принято, так и напиши.

                        "next_step":
                        Только конкретные следующие действия, которые были явно согласованы или поручены участникам.
                        Для каждого действия по возможности указывай исполнителя и срок.
                        Не придумывай действия клиента или менеджера.
                        Не добавляй предполагаемые действия, которые участники не обсуждали.

                        "date":
                        Только дата проведения разговора.
                        Не добавляй сюда даты следующих встреч, дедлайны или сроки.
                        Если дата разговора отсутствует, используй "Информация отсутствует".

                        "needs":
                        Только потребности, требования, задачи или ожидаемый результат клиента, явно выраженные в разговоре.
                        Не помещай сюда действия менеджера, договорённости или общие предположения.
                        Если клиент чего-либо хочет, требует или считает необходимым — это может быть указано здесь.

                        "risks":
                        Только явно названные участниками риски, ограничения, проблемы или препятствия.
                        Не превращай собственный логический вывод в риск.
                        Если участник сказал, что что-то может не сработать, это можно указать как риск.
                        Если риск только предполагается моделью, не добавляй его.

                        "managers_mistakes":
                        Только конкретные действия или предложения менеджера, которые в самом разговоре были явно отвергнуты, исправлены или показаны как неподходящие.
                        Не называй действие ошибкой только потому, что оно кажется тебе неудачным.
                        Если явной ошибки менеджера нет, используй "Информация отсутствует".
                        Всегда сохраняй имя менеджера, если оно известно.

                        "important":
                        Важные факты из разговора, которые могут понадобиться при дальнейшей работе с клиентом и не были полноценно отражены в других полях.
                        Сюда можно включать количество филиалов, текущую инфраструктуру, участников принятия решения, ограничения клиента, конкурентов, бюджетные ориентиры и другие существенные факты.
                        Не добавляй сюда предположения.

                        ПРОВЕРКА ПЕРЕД ОТВЕТОМ:

                        Перед формированием JSON мысленно проверь каждое утверждение:
                        1. Есть ли это непосредственно в транскрипте?
                        2. Кто именно это сказал?
                        3. Это факт, договорённость или только предположение?
                        4. Не изменил ли я смысл исходной фразы?
                        5. Не превратил ли я намерение в принятое решение?
                        6. Не добавил ли я причинно-следственную связь от себя?
                        7. Если информации недостаточно — нужно написать "Информация отсутствует".

                        ВАЖНО:
                        Сохраняй точность важнее полноты.
                        Лучше написать "Информация отсутствует", чем заполнить поле предположением.

                        Транскрипт:
                        """
                        f"{content}"
                    )
                }
            ]
        }

        with requests.Session() as session:
            with session.post(self.base_url, json=data, headers=self.headers) as resp:
                result = resp.json()
                data = json.loads(result["choices"][0]["message"]["content"])
                
        if ("result" in data and len(data["result"]) > 0) and ("next_step" in data and len(data["next_step"]) > 0) and ("date" in data and len(data["date"]) > 0) and ("needs" in data and len(data["needs"]) > 0) and ("risks" in data and len(data["risks"]) > 0) and ("managers_mistakes" in data and len(data["managers_mistakes"]) > 0) and ("important" in data and len(data["important"]) > 0):
            return data
        else:
            return "Попробуйте ещё раз"
                    
                

        